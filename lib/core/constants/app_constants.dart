/// Konstanta umum aplikasi OTW MBENGKEL
class AppConstants {
  // ==================== APP INFO ====================
  /// Nama aplikasi
  static const String appName = 'OTW MBENGKEL';

  /// Versi aplikasi
  static const String appVersion = '1.0.0';

  // ==================== RESPONSIVE BREAKPOINTS ====================
  /// Breakpoint untuk mobile devices (< 600px)
  static const double mobileBreakpoint = 600;

  /// Breakpoint untuk tablet devices (600px - 1200px)
  static const double tabletBreakpoint = 1200;

  /// Breakpoint untuk desktop devices (> 1200px)
  static const double desktopBreakpoint = 1200;

  // ==================== PADDING & MARGIN ====================
  /// Padding/Margin ekstra kecil
  static const double paddingXSmall = 4;

  /// Padding/Margin kecil
  static const double paddingSmall = 8;

  /// Padding/Margin normal
  static const double paddingNormal = 16;

  /// Padding/Margin medium
  static const double paddingMedium = 24;

  /// Padding/Margin besar
  static const double paddingLarge = 32;

  /// Padding/Margin ekstra besar
  static const double paddingXLarge = 48;

  // ==================== BORDER RADIUS ====================
  /// Border radius kecil (rounded corners)
  static const double radiusSmall = 8;

  /// Border radius normal
  static const double radiusNormal = 12;

  /// Border radius medium
  static const double radiusMedium = 16;

  /// Border radius besar (untuk circular elements)
  static const double radiusLarge = 24;

  // ==================== ICON SIZES ====================
  /// Ukuran icon kecil
  static const double iconSmall = 20;

  /// Ukuran icon normal
  static const double iconNormal = 24;

  /// Ukuran icon medium
  static const double iconMedium = 32;

  /// Ukuran icon besar
  static const double iconLarge = 48;

  // ==================== DURASI ANIMASI ====================
  /// Durasi animasi cepat (milliseconds)
  static const int durationFast = 200;

  /// Durasi animasi normal (milliseconds)
  static const int durationNormal = 300;

  /// Durasi animasi lambat (milliseconds)
  static const int durationSlow = 500;
}
